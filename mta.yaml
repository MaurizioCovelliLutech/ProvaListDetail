_schema-version: "3.2"
ID: ux402listdetail
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: ux402listdetail-destination-content
  type: com.sap.application.content
  requires:
  - name: ux402listdetail-destination-service
    parameters:
      content-target: true
  - name: ux402listdetail-repo-host
    parameters:
      service-key:
        name: ux402listdetail-repo-host-key
  - name: ux402listdetail-uaa
    parameters:
      service-key:
        name: ux402listdetail-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: ux402listdetail_html_repo_host
          ServiceInstanceName: ux402listdetail-html5-srv
          ServiceKeyName: ux402listdetail-repo-host-key
          sap.cloud.service: ux402listdetail
        - Authentication: OAuth2UserTokenExchange
          Name: ux402listdetail_uaa
          ServiceInstanceName: ux402listdetail-xsuaa-srv
          ServiceKeyName: ux402listdetail-uaa-key
          sap.cloud.service: ux402listdetail
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: ux402listdetail-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: ux402listdetail-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - ux402listdetail.zip
      name: ux402listdetail
      target-path: resources/
- name: ux402listdetail
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: ux402listdetail-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: ux402listdetail-destination-service
    service-plan: lite
- name: ux402listdetail-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: ux402listdetail-xsuaa-srv
    service-plan: application
- name: ux402listdetail-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: ux402listdetail-html5-srv
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
